# URL of the deployed backend (Render), no trailing slash.
# Used for both REST calls and the Socket.io client (see src/lib/config.js).
# Local dev falls back to http://localhost:3005 if this is unset.
VITE_API_URL=https://your-backend.onrender.com
